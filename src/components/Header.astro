---
import ClockHora from "./header/ClockHora.astro";
import KeyBoardSearch from "./header/KeyBoardSearch.astro";
import Spin from "./spin.astro";
import StarSeach from "~/icons/StarSeach.astro";
import StarIF from "~/icons/StarIF.astro";
import StarTime from "~/icons/StarTime.astro";
---

<header
  class="flex justify-between gap-x-6 mb-8 md:order-last md:flex-col md:gap-8 md:gap-x-0 md:items-center md:w-100 md:h-11/12 py-4 md:justify-start md:mt-0"
>
  <article
    class=` relative flex flex-col justify-center items-center  cursor-pointer md:mr-60 `
  >
    <StarSeach class="absolute inset-0 w-full h-full fill-current z-0" />
    <KeyBoardSearch />
  </article>
  <article class="flex flex-col justify-center items-center h-16 relative">
    <StarTime class="absolute inset-0 w-full h-full fill-current z-0" />

    <ClockHora />
  </article>

  <article
    class="hidden md:flex relative   flex-col justify-center items-center  cursor-pointer md:mr-60"
    id="imagine"
  >
    <StarIF class="absolute inset-0 w-full h-full fill-current z-0" />
    <p id="text" class="px-4 text-sm transition-all duration-500 text-white z-10">
      If u can imagine it, u can program it
    </p>
       

  </article>

  <!-- <article class="hidden md:block h-16 px-8 md:mr-60 relative"  id="imagine">
    <StarIF class="absolute inset-0 w-full h-full fill-current z-0" />
    <p id="text" class="text-sm transition-all duration-500 text-white z-10">
      If u can imagine it, u can program it
    </p>
  
    
  </article> -->
</header>

<script>
  const imagine = document.getElementById("imagine");
  const parrafo = document.getElementById("text");

  let texto = true;

  imagine?.addEventListener("click", (e) => {
    texto = !texto;

    const newTexto = texto
      ? "If u can imagine it, u can program it"
      : "Puede imaginarlo, puede programarlo";

    parrafo.innerText = newTexto;
  });
</script>

<style>
  article {
    width: 130.23px;
    height: 130px;
    background-size: cover;
    background-position: center;
  }
</style>
